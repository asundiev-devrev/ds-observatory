{
  "name": "ds-observatory",
  "version": "0.1.0",
  "description": "Design system observability — Figma component usage analytics",
  "type": "module",
  "bin": {
    "ds-observatory": "./dist/cli/index.js"
  },
  "scripts": {
    "dev": "tsx src/cli/index.ts",
    "build": "tsc",
    "build:worker": "tsc -p tsconfig.worker.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260713.1",
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4",
    "wrangler": "^4.110.0"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "dotenv": "^17.4.2"
  }
}
